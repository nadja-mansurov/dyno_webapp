
name: CI


on:
  push:
    branches: 
      - feature/#1_add_cypress

jobs:
  install:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/setup-node@v2
  
  cypress_run:
    runs-on: ubuntu-latest

    steps: 
    - name: Cypress_run
      # run: cypress-io/github-action@v2
      run: npm run cy:run 

    #steps:
    #- name: Cypress run
    #- uses: actions/checkout@v2
    #- uses: cypress-io/github-action@v2.2.7

    #steps:
    #- uses: main
    #- run: npm run build
    #- run: npm run start
    #- run: 'http://localhost:4200/'
    #- run: baseUrl=http://localhost:4200
    #- run: |
     #     cypress/integration/checkboxes.spec.ts
     #     cypress/integration/displayButtons.spec.ts







        

   
      
        
